version: "3.9"

services:
  kino:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PUSHOVER_USER=${PUSHOVER_USER}
      - PUSHOVER_TOKEN=${PUSHOVER_TOKEN}
    # mount code for local development (optional)
    volumes:
      - ./:/app
    restart: unless-stopped
